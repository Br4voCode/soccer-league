API_TARGET=local
API_URL_LOCAL=http://localhost:8080
API_URL_REMOTE=https://soccer-league-1.onrender.com

# Debe ser EXACTAMENTE el mismo valor que JWT_SECRET en el backend Go (Api/.env) —
# este frontend solo verifica el JWT, quien lo emite es el backend.
JWT_SECRET=
